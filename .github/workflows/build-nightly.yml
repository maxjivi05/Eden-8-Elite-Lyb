name: Sync Fork

on:
  schedule:
    # This runs the job automatically every 6 hours.
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Sync Fork Action
        uses: repo-sync/github-sync@v2
        with:
          source_repo: "https://github.com/pflyly/eden-nightly.git"
          source_branch: "main"
          destination_branch: "main"
          github_token: ${{ secrets.PAT }}```

---

### Step 3: Update the Build Workflow

This is your main build file. It is now much simpler and cleaner.

1.  Replace the **entire contents** of your `.github/workflows/build-nightly.yml` file with this code.

```yaml
name: Build Lyfb Version

concurrency:
  group: build-eden-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build_android:
    runs-on: ubuntu-latest
    name: "Android (Lyfb)"
    env:
      TARGET: Lyfb
      CCACHE_DIR: ${{ github.workspace }}/.ccache
      CCACHE_COMPILERCHECK: content
      CCACHE_SLOPPINESS: time_macros
    steps:
      - name: Checkout Your Repository (including patch file)
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y ccache glslang-tools libvulkan-dev python3-requests

      - name: Clone Latest Eden Source
        run: |
          git clone 'https://git.eden-emu.dev/eden-emu/eden.git' ./eden
          echo "Successfully cloned latest Eden source."

      - name: Apply Android Patch
        working-directory: ./eden
        run: |
          # This is the standard, reliable way to apply a patch.
          git apply ../.github/patches/android-fix.patch
          echo "Android patch applied successfully."

      - name: Restore ccache
        uses: actions/cache/restore@v4
        id: restore-ccache-cache
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ${{ runner.os }}-android-ccache-${{ env.TARGET }}-${{ github.run_id }}
          restore-keys: |
            ${{ runner.os }}-android-ccache-${{ env.TARGET }}-

      - name: Compile Eden Android
        run: |
          chmod +x ./eden-android.sh
          ./eden-android.sh

      - name: Save ccache
        if: ${{ github.ref_name == 'main' }}
        uses: actions/cache/save@v4
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ${{ steps.restore-ccache-cache.outputs.cache-primary-key }}

      - name: Upload Artifact
        uses: actions/upload-artifact@v5.0.0
        with:
          name: eden-android-${{ env.TARGET }}
          path: eden/src/android/artifacts/
